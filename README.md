# SPLITTA - React native project

### Setup

```
$ git clone https://github.com/davjo664/splitta
$ cd splitta
$ npm install
$ react-native link
$ react-native link react-native-qrcode-scanner
$ react-native link react-native-permissions

```